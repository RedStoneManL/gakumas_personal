# ARM64 / 昇腾适配验收

日期：2026-09-15。代码、打包、真实训练和恢复的本地验收已完成。**没有昇腾服务器连接，ARM64/CANN/NPU 算子与 8 卡 HCCL 尚未实测。**

本地环境：Windows AMD64、Python 3.12.7、PyTorch 2.9.1+cu128、Node 26.6.0。完整入口运行采用 CPU；部分已有模型合批回归实际使用本机 CUDA。

## 最新 checkpoint、signed-credit 与观测台交付

- 封存完整 `latest.pt`：batch 14、190,584 个有效决策，保留模型、32 分位头和 Adam。原始字节 SHA256 见 `checkpoints/provenance.json`，通过兼容模型加载。
- 最新源码单元回归：完整训练/控制组 53 项通过，搜索组 53 项通过、3 项跳过；观测台数据与真实 HTTP 接口 23 项通过。共 129 项不同测试通过。JavaScript 语法检查通过。
- 双进程正负贡献 + KL 早停价值补训与单进程模型/Adam 对照通过，参数最大差异约 8.94e-8；通信仍使用本地测试专用 Pipe，不是 HCCL 实测。
- 当前 checkpoint 的真实 CPU 预检通过：两个公开搜索根、四场真实短考试、正负贡献/分离搜索目标/价值损失更新、Adam 与 RNG 恢复。预检使用新 Adam；完整训练入口使用 `--initial` 恢复源 Adam。
- 观测台支持显式运行目录、892 个图标、日志、最新学习指标与下一批资源请求；1024 个采样进程等数值只验证配置传播，没有实际申请该规模。
- 已构建含 checkpoint / 观测台的独立 ZIP 并进行新目录回归。用户要求优先推送，完整长训、策略提升和 ARM64/NPU/HCCL 留待目标服务器验证；以下旧版完整训练/恢复记录保持其原范围。

## 可配置规模更新（前一版）

以下为移除规模上限后的本地验证；学习卡数、采集/搜索进程、推理批量、PPO 批量和 CPU 线程数没有应用层固定上限，仍检查正整数、微批与全局批量关系以及单机设备映射。

| 检查 | 实际结果与范围 |
|---|---|
| 源码回归 | 核心及控制组 53 项通过；搜索/多进程组 30 项通过、3 项跳过；性能配置和其他运行入口 33 项通过。共 116 项不同测试通过。 |
| 超过旧上限的配置 | 32 个学习进程映射；1024 个采集进程、512 个搜索进程、1024 条推理合批、100000 局、32768 全局批量、2048 微批与 128 CPU 线程的配置传播通过。高数量构造采用模拟，不实际申请这些资源。 |
| 配置与状态 | 配置生成器保留算法设置并拒绝覆盖；完整育成初始配置、热控制、迁移与 Notebook 接受更大正整数，非法值不覆盖上一有效状态。自动进程数不再截断到 10。 |
| 解压包回归 | 新目录内核心组 53 项通过、搜索组 30 项通过 / 3 项跳过；修复了新纳入回归的 Notebook/迁移测试对源码目录布局的假设。 |
| 真实运行 | 两个公开历史搜索根、真实短考试、Adam 更新和 RNG 恢复通过；完整育成以两个采集进程运行两次，每次 2 局，第二次换临时目录恢复，最终 iteration=2、episodes_seen=4、seed_cursor=92000004。 |

本次没有运行 32 卡或数百进程容量测试，没有重新进行完整搜索长训或策略强度评估。新的 [规模验收 JSON](validation/scaling-acceptance.json) 记录候选包、日志和源码哈希；以下初始移植记录保留其当时的测试与训练证据。

## 初始移植验收（本地提交 4086e30）

| 检查 | 实际结果与范围 |
|---|---|
| 核心回归 | 66 项通过：设备选择、随机状态、优化器恢复、PPO、真实模型合批、OOM、运行配置、并行采样、持久化与恢复。NPU 专用分支采用设备模拟，不是 NPU 算子实测。 |
| 搜索及多进程回归 | 25 项通过、3 项明确跳过；覆盖异步采样、公开历史搜索、软预算、四局目标、分位价值头、GRU 与分布式更新数学。加上核心回归，共 91 项不同测试通过。 |
| 解压包内回归 | 同一 ZIP 解压到新目录，核心子集 31 项通过；搜索组 25 项通过、3 项跳过。未把这个重复运行另加到 91 项中。 |
| GRU 对照 | FP32 输出、输入梯度和全部参数梯度与原 PackedSequence 实现对照通过；使用同一组继承参数和变长输入。 |
| 两进程更新数学 | 搜索学习器连续两次更新，含 PPO/搜索/分位损失、奇数尾批、空分片和 Adam 恢复，模型最大差异约 1.49e-8；完整育成更新最大差异约 7.28e-12。 |
| 真实 Arena/搜索预检 | 两个合格公开历史搜索根；真实 recorder 未被搜索改变；在真实短考试中执行搜索动作，以实际终分更新；81 个参数张量改变，随机状态恢复通过。 |
| 完整育成入口 | 解压包执行两次独立启动、每次 2 个采样进程/2 局；第二次使用新的临时目录恢复。最终 iteration=2、episodes_seen=4、seed_cursor=92000004。正常选拔失败仍保留实际终局评价。 |
| 最新搜索入口 | 解压包完成 1 个训练批：156 个有效决策，84 条搜索记录 + 74 条 PPO 记录（含强制动作记录），20 次 Adam 更新；完成保存、收尾评估与同源断点恢复。 |
| 来源完整性 | 222 个接收文件存在；完整 Arena 的 700 个冻结文件逐个哈希一致；交付 Python 源文件语法检查通过，安装脚本保持 LF 换行。 |

两进程本地数学测试使用**测试专用 Pipe 序列化通信**，执行实际分片、归一化、梯度求和与优化器代码。本机原生 Gloo 初始化失败，沙箱外重试结果相同，因此不能把这项测试当作 Gloo/HCCL 通信验收。三个跳过项为该原生 Gloo 测试和两个依赖旧本机环境的 Windows 监督脚本测试。

## 可追溯证据与复跑

- [结构化验收记录](validation/portable-acceptance.json) 保存实跑候选 ZIP、源码、日志哈希和各阶段结果。
- 完整原始日志位于工作区 `rl/ascend/artifacts/validation-hif-training-w3ueea_z/`。
- 工作区重建包：`python rl/hif-colab/build_training_bundle.py --target ascend`。
- 使用已安装本项目依赖的 Python，执行 `python rl/ascend/validate_bundle.py` 可复跑解压包测试、训练和恢复；这是小规模实际训练，会创建临时运行目录。
- 每轮发布的最终交付包会补充说明和 JSON 证据；训练代码、配置、引擎与测试核对相应实跑候选包。ZIP 整包哈希以旁边的 `.sha256` 文件为准，包内 manifest 逐文件校验。

## 服务器仍须验证

按 [README](README.md) 顺序执行环境检查、单卡搜索、8 卡搜索/梯度求和和完整育成预检，之后再从小批量升到 96 个采样进程 + 24 个搜索进程。预检失败时保留 JSON 和 stderr。

当前证据没有覆盖真实 ARM64/NPU、8 卡原生通信、大规模内存峰值、吞吐加速、策略收敛或游戏规则的全量实证。上述少量训练与评估用于验证运行链路；完整育成失败路线和五局小评估不能作为策略强度结论。8 卡参与更新阶段，采样期的策略推理仍集中在 rank 0。
